#include <string.h>

/**
 * _strlen - function that returns the length of a string.
 * @s : char to check
 *
 */

int _strlen(char *s)
{
	int a;

	for (a = 0; s[a] != '\0'; a++)
	{
	}

	return (a);
}

